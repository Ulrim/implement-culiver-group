{
  "name": "culiver-group-site",
  "private": true,
  "description": "CULIVER GROUP corporate site — static pages generated by tools/build_pages.py, plus Vercel serverless functions under api/. This package.json exists only for the api/ functions' dependencies; there is no build step for the site itself.",
  "dependencies": {
    "@vercel/blob": "^2.6.1"
  }
}
